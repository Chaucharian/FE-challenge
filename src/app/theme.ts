import { extendTheme } from "native-base";

export const theme = extendTheme({
  colors: {
    primary: "#1E1E1E",
  },
  fonts: {
    heading: "Urbanist",
    body: "Urbanist",
    mono: "Urbanist",
  },
  config: {
    initialColorMode: "dark",
  },
});
